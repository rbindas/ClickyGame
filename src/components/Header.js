import React from "react";

const Header = () => (
  <header className="header">
    <h1>Clicky Game!</h1>
    <h2>Click on the image to earn points, but don't
    click on any image more than once</h2>
  </header>
);
export default Header;